# 세션 핸드오프 - 2026-02-26 (2차)

**날짜**: 2026-02-26
**상태**: 미push 커밋 2개 있음 + 미구현 작업 1개

---

## ⚠️ 최우선: 미push 커밋 배포

아래 커밋 2개가 로컬에만 있음. **먼저 push 후 Vercel 배포 확인 필요.**

```bash
git push
```

| 커밋      | 내용                                      |
| --------- | ----------------------------------------- |
| `b2febe3` | 기수별 자료제출 마감 팝업 시스템          |
| `4183edb` | 기수 생성 시 자료제출 마감 팝업 자동 등록 |

---

## 이번 세션 완료 작업 (이미 push됨)

### 1. 홈페이지 제작요청 - 도메인 주소 필드 추가 (`dea9441`)

- 도메인 정보 섹션에 "도메인 주소" 필수 입력 필드 추가
- 예시: `www.mybrand.co.kr | mybrand.com | mybrand.kr`
- Prisma 스키마 `도메인주소` 필드 추가 + DB push 완료
- 슬랙 알림 메시지에도 도메인 주소 포함

### 2. 사용자 관리 기수 필터 드롭다운 (`6215bed`)

- 가로 스크롤 탭 → Select 드롭다운 방식으로 변경
- 실제 가입자가 있는 기수만 표시 (`activeCohorts` useMemo)
- 각 기수에 인원수 표시 (예: `26-1기 (5명)`)

### 3. 슬랙 수정이력 기록 (`d49c28f`)

- 연락처 변경 (`/api/admin/users/update-phone`): 이전→새 번호 슬랙 기록
- 수신동의 변경 (`/api/user/settings`): SMS/이메일 동의 변경 시 슬랙 기록
- 제출정보 변경은 기존 submission route에서 이미 처리됨

### 4. 기수 생성 마감일 3주 자동계산 (`47724d1`)

- 기수 추가/수정 다이얼로그: 교육시작일 선택 시 자료제출마감일 = +21일 자동
- 라벨에 "(교육시작일 기준 3주 자동계산)" 안내 표시

### 5. 기수별 자료제출 마감 팝업 시스템 (미push)

#### 스키마 변경

- `SystemAlert` 모델에 `cohortId String?`, `phoneNumber String?` 추가
- DB push 완료

#### 알림 API 변경 (`/api/alerts/active`)

- 사용자 `cohortId` 조회 후 필터링:
  - `cohortId = null` → 전체 공개
  - `cohortId = 사용자기수ID` → 해당 기수만 노출

#### 팝업 UI (`components/ui/system-alert-modal.tsx`)

- 기수 전용 팝업: 오렌지/빨간 그라데이션 헤더
- 마감일 강조 박스 (D-day 자동계산)
- 전화번호 + 복사버튼 + 모바일 tel: 링크
- 24시간 숨김 기능 유지

#### DB 데이터

- **26-1기** 자료제출마감일 수정: `2026-02-04` → `2026-03-04`
- **26-1기 팝업** 생성: 마감 2026-03-04(수), 노출 2026-02-26~03-04
- **26-2기 팝업** 생성: 마감 2026-03-19(목), 노출 2026-02-26~03-19
- 담당자 연락처: `010-9897-9834`

#### 기수 생성 자동화

- `/api/admin/cohorts/create`: 기수 생성 시 팝업 `SystemAlert` 자동 등록
  - 노출기간: 교육시작일 ~ 자료제출마감일
  - 마감일 한국어 형식: "2026년 3월 19일(목)"
  - 담당자 번호 자동 포함

---

## ❌ 미구현 작업 (다음 세션 구현)

### 카드 정보 UI 변경 (`app/dashboard/homepage/page.tsx`)

**현재**: 카드 앞면/뒷면 사진 업로드 방식
**변경 요청**:

- 카드 앞면 사진 → **신용카드 사진 1장** (카드번호 보이도록)
- 카드 유효기간 → 사진 아닌 **텍스트 입력** (MM/YY 이미 있음, 위치 재배치)
- CVC 번호 → 새로 **텍스트 입력** 필드 추가
- 카드 뒷면 사진 업로드 **제거**

**관련 파일**:

- `app/dashboard/homepage/page.tsx` (UI 수정)
- `app/api/homepage/route.ts` (스키마/검증에서 뒷면URL 제거 또는 optional 처리)
- `lib/schemas/submission.schema.ts` (CVC 필드 추가 검토)
- `prisma/schema.prisma` (해외결제카드CVC 필드 추가 검토)

---

## 주요 기수 ID (DB)

| 기수명 | cohortId                    |
| ------ | --------------------------- |
| 26-1기 | `cmko3tjpo0000i804phd4m0wf` |
| 26-2기 | `cmm2sfy5t0000lb04e34aaos8` |

---

## 프로젝트 정보

- **경로**: `F:\startpackage`
- **GitHub**: `git@github-pola2025:pola2025/startpackage.git`
- **배포**: Vercel 자동배포 (push 시)
- **프레임워크**: Next.js 15 + Prisma + NextAuth

**작성일**: 2026-02-26
