"use client";
import { FormEvent,useState } from "react";
import Link from "next/link";
import styles from "../education.module.css";
export default function ExtensionPage(){const[phone,setPhone]=useState("");const[error,setError]=useState("");const[done,setDone]=useState(false);const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/education/extension",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({phone})});const d=await r.json();if(!r.ok){setError(d.error||"신청할 수 없습니다.");return}setDone(true)}catch{setError("네트워크 연결을 확인해주세요.")}finally{setBusy(false)}}return <main className={styles.page}><section className={styles.desktop}><div className={styles.brand}><small>EDUCATION ACCESS</small><h1>교육자료<br/><em>열람 연장신청</em></h1><p>기존에 등록한 전화번호를 확인한 뒤 관리자 검토 대기 상태로 접수됩니다.</p></div><div className={styles.panel}>{done?<><h2>신청이 접수되었습니다.</h2><div className={styles.status}><strong>관리자 검토 대기</strong><p>승인 전에는 열람기간이 바뀌지 않습니다. 담당자 확인 후 연장됩니다.</p></div><Link className={styles.back} href="/education">입장 화면으로 돌아가기</Link></>:<><h2>등록정보 확인</h2><p>수강 등록 시 사용한 본인 전화번호를 입력하세요.</p><form className={styles.form} onSubmit={submit}><label htmlFor="phone">본인 전화번호</label><input id="phone" type="tel" inputMode="tel" autoComplete="off" placeholder="010 1234 5678" value={phone} onChange={e=>setPhone(e.target.value)} maxLength={13}/><button disabled={busy}>{busy?"확인 중":"연장신청 접수"}</button><p className={styles.error} role="alert">{error}</p></form><Link className={styles.extend} href="/education">교육자료 입장으로 돌아가기</Link></>}</div></section><section className={styles.mobile}><div className={styles.pcIcon}/><h1>PC 또는 노트북에서<br/>신청해주세요.</h1><p>교육자료와 연장신청은 PC 화면에서 제공합니다.</p></section></main>}
